@model Personne
@{
    ViewData["Title"] = "SubmitPersonne";
}
<div>

    <h1>Personne issue du formulaire</h1>
    <h2>Details de la personne n°@Model.Id</h2>
    <div class=row>
        <div class="col">@Model.Nom</div>
        <div class="col">@Model.Prenom</div>
        <div class="col">@Model.Telephone</div>
    </div>


</div>